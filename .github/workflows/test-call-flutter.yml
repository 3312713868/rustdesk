name: Test Call Flutter Build
on:
  workflow_dispatch:
jobs:
  call:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: false
      upload-tag: "test"